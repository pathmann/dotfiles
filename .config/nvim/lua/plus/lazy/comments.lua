return {
  "numToStr/Comment.nvim",

  opts = {
    toggler = {
      line = "<leader>//",
      block = "<leader>/*",
    },
    opleader = nil,
    extra = nil,
    mappings = {
      extra = false,
    }
  }
}
